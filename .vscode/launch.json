{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Function",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/runLocal.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}